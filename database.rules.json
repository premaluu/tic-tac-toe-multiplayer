{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.child('players/X/uid').val() === auth.uid) || (data.exists() && (data.child('players/X/uid').val() === auth.uid || data.child('players/O/uid').val() === auth.uid || (data.child('status').val() !== 'playing' && newData.child('players/O/uid').val() === auth.uid))))"
      }
    }
  }
}
